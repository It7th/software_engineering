# Контекст картинок по билетам

## Билет 3
- `image105.png`
  - До: Цена ошибки:
  - После: Модели разработки ПО:

## Билет 3
- `image40.png`
  - До: Waterfall (каскадная)
  - После: Основная ценность - точное выполнение ТЗ. | Характерна для it-аутсорса.

## Билет 3
- `image30.png`
  - До: V-модель
  - В абзаце: Схожа с каскадной, но план тестирования строится на каждом этапе => меньше неприятных сюрпризов на поздних этапах.
  - После: Оправдана в проектах, где важна надежность. | 4 Модели разработки ПО: спиральная, инкрементная, итерационная

## Билет 4
- `image78.png`
  - До: Разработка/конструирование - создание следующей версии продукта. | Оценка и проверка - заказчик тестирует продукт, вносятся корректировки.
  - После: Особенности: | Не требует подробного ТЗ на старте, т.к. продукт уточняется в процессе.

## Билет 4
- `image152.png`
  - До: Инкрементная модель | Инкрементная модель - это модель разработки программного обеспечения, при которой продукт создается поэтапно, добавляя новые функциональные части (инкременты) к уже существующей версии. Каждая часть может быть протестирована и использована пользователем до завершения всего проекта. Еще раз подчеркнем (это важно), что каждый инкремент — это рабочая часть продукта с определенной функциональностью. После завершения очередного инкремента продукт становится более полнофункциональным и может быть представлен пользователю.
  - После: Особенности: | Первая версия продукта создается быстро, и заказчик может начать пользоваться ей практически сразу.

## Билет 4
- `image89.png`
  - До: Итерационная (она же итеративная) модель | Итерационная модель - это модель разработки программного обеспечения, при которой продукт создается через последовательные итерации. Каждая итерация включает планирование, разработку и тестирование, что позволяет постепенно улучшать продукт и вносить изменения на основе обратной связи.
  - После: Еще одна картинка для большего понимания.

## Билет 4
- `image14.png`
  - До: Еще одна картинка для большего понимания.
  - После: Особенности:

## Билет 5
- `image21.png`
  - До: Kanban – метод управления разработкой, при котором задачи распределяются равномерно между всеми участниками команды разработки, реализует принцип «точно в срок», и ограничивается одновременное максимальное количество выполняемых задач. | Простой пример реализации доски Kanban представлен ниже. В общем случае каждый столбик является отдельным этапом жизненного цикла разработки.
  - После: Основные принципы: | Визуализация – доска с карточками-задачами (user-stories в разработке продукта). Доска может быть физическая или виртуальная.

## Билет 9
- `image107.png`
  - До: что процесс не может выполняться, даже если процессору кроме него больше нечем заняться.

## Билет 10
- `image45.png`
  - До: У ядра же - блокирующие вызовы делает ядро, но больше затрат на переключение , создание, удаление потоков, а также старые потоки переиспользуются.

## Билет 10
- `image64.png`
  - До: опишем ряд самых основных функций, чтобы дать представление о том, как они работают.
  - После: Новый поток создается с помощью вызова функции pthread_create. В качестве значения функции возвращается идентификатор только что созданного потока. Этот вызов намеренно сделан очень похожим на системный вызов fork (за исключением параметров), а идентификатор потока играет роль PID, главным образом для идентификации ссылок на потоки в других вызовах.

## Билет 12
- `image83.png`
  - До: потоки создаются Clone() | Либо в текущем либо в новом процессе создаются
  - После: **Дескриптор** процесса содержит **информацию** о **процессе**, которая необходима ядру ОС в **течение всего жизненного цикла** процесса независимо от того в каком он состоянии находится.

## Билет 12
- `image143.png`
  - До: proc это где хранится инфа о процессах
  - После: 13 Рождение, смерть и жизненный цикл процессов в unix-like системах.

## Билет 13
- `image9.png`
  - До: 5) Первым управление получит child (?) (как будто это не всегда правда, но в презе так)
  - В абзаце: Тут описан процесс как fork, дальше exec происходит
  - После: Exec | Загружает бинарный файл в адресное пространство процесса.

## Билет 13
- `image32.png`
  - До: Выполнение с точки входа в программу. | После fork() не происходит копирования страниц. тут по хитрому - чтобы место экономить - используется адресное родителя, но с пометкой, что только чтение. Тогда на запись будет copy-on-write т.к копировать долго и дорого
  - После: Смерть | По системному вызову exit или сигнал завершения от другого процесса (kill)

## Билет 13
- `image3.png`
  - До: Мы можем знать статус завершения вызвав $? - там код завершения последней команды
  - В абзаце: Схема перехода состояний процесса
  - После: Состояния | Ready - готов к исполнению. Ждет выделение проц времени планировщиком.

## Билет 16
- `image71.png`
  - До: 16 Процессорный кэш. Механизм кэширования. Стратегии кэширования и вытеснения. Прогрев. | Пока память была примерно как время работы проца - кэш был не нужен. Но сейчас проц быстрее памяти.
  - После: Программа копируется в ram, а оттуда в проц. Проц ждет этих событий.

## Билет 16
- `image5.png`
  - До: Программа копируется в ram, а оттуда в проц. Проц ждет этих событий.
  - После: Кэш очень быстрая память и рядом с процессором. Но ее не много. | На 1 уровне инструкции и данные.

## Билет 16
- `image136.png`
  - До: Чем выше уровень, тем наиболее вероятно понадобится дальше процу. При том чем выше уровень, тем меньше там памяти зачастую.
  - После: Есть метрика Hit-Rate - отношение сколько попали в кэш и нашли, к тому сколько обращались к кэшу.

## Билет 17
- `image117.png`
  - До: Прогрев - процесс заполнения кэша актуальными данными для увеличения hit-rate.
  - В абзаце: 17 Оперативная память. Виртуальная память. Организация памяти.
  - После: При реализации страничной виртуальной памяти виртуальное адресное пространство делится на части одинакового фиксированного размера, называемые виртуальными страницами. Если виртуальное адресное пространство какого-то процесса не кратно размеру страницы, то последняя страница дополняется фиктивной областью. Физическая оперативная память также делится на части такого же размера, называемые физическими страницами. Размер страницы выбирается кратным степени двойки. Копия всех виртуальных страниц хранится на диске.
- `image46.png`
  - До: Прогрев - процесс заполнения кэша актуальными данными для увеличения hit-rate.
  - В абзаце: 17 Оперативная память. Виртуальная память. Организация памяти.
  - После: При реализации страничной виртуальной памяти виртуальное адресное пространство делится на части одинакового фиксированного размера, называемые виртуальными страницами. Если виртуальное адресное пространство какого-то процесса не кратно размеру страницы, то последняя страница дополняется фиктивной областью. Физическая оперативная память также делится на части такого же размера, называемые физическими страницами. Размер страницы выбирается кратным степени двойки. Копия всех виртуальных страниц хранится на диске.
- `image128.png`
  - До: Прогрев - процесс заполнения кэша актуальными данными для увеличения hit-rate.
  - В абзаце: 17 Оперативная память. Виртуальная память. Организация памяти.
  - После: При реализации страничной виртуальной памяти виртуальное адресное пространство делится на части одинакового фиксированного размера, называемые виртуальными страницами. Если виртуальное адресное пространство какого-то процесса не кратно размеру страницы, то последняя страница дополняется фиктивной областью. Физическая оперативная память также делится на части такого же размера, называемые физическими страницами. Размер страницы выбирается кратным степени двойки. Копия всех виртуальных страниц хранится на диске.

## Билет 18
- `image114.png`
  - До: При сегментном распределении памяти виртуальное адресное пространство делится на части, называемые сегментами, размер которых определяется смысловым значением содержащейся в них информации (Память процесса делится на логические сегменты: код, данные, стек, heap). Максимальный размер сегмента определяется разрядностью процессора, для 32-разрядного процессора он равен 4 Гбайт. Сегменты никак не упорядочиваются друг относительно друга. Виртуальный адрес задается парой чисел: номером сегмента и смещением внутри сегмента. При загрузке процесса в оперативную память помещается только часть его сегментов, полный образ виртуального адресного пространства находится на диске. Для каждого загружаемого сегмента подыскивается непрерывный участок свободной физической памяти достаточного размера. | 18 Выделение памяти по требованию. Страничный кэш. Опережающее чтение. Overcommit. OOM-killer и его задачи.
  - После: Это не отдельный способ маппинга, а расширение paging-модели | Что происходит на практике

## Билет 18
- `image129.png`
  - До: Недостаток: | первый доступ к странице медленный (page fault)
  - После: 2.1. Not present / Unmapped (не отображена)Виртуальная страница: | существует в адресном пространстве

## Билет 18
- `image92.png`
  - До: неактивные части heap | старые данные процесса
  - После: Что хранится: | содержимое файлов

## Билет 18
- `image135.png`
  - До: да, в любой момент | если страница чистая (clean)

## Билет 18
- `image72.png`
  - До: если страница чистая (clean)

## Билет 18
- `image123.png`

## Билет 18
- `image68.png`

## Билет 19
- `image49.png`
  - До: синхронизация баз данных | Как общаются -
  - После: ARPANET похоже стал wan, но уже с поддержкой правительства сша | Lan

## Билет 19
- `image96.png`
  - До: ARPANET похоже стал wan, но уже с поддержкой правительства сша
  - В абзаце: Lan
  - После: 20 Коммутация каналов и пакетов. Топология сетевых связей. Адресация. Модель клиент-сервер.

## Билет 19
- `image51.png`
  - До: ARPANET похоже стал wan, но уже с поддержкой правительства сша | Lan
  - После: 20 Коммутация каналов и пакетов. Топология сетевых связей. Адресация. Модель клиент-сервер.

## Билет 20
- `image127.png`
  - До: 20 Коммутация каналов и пакетов. Топология сетевых связей. Адресация. Модель клиент-сервер.

## Билет 20
- `image24.png`
  - До: 20 Коммутация каналов и пакетов. Топология сетевых связей. Адресация. Модель клиент-сервер.

## Билет 20
- `image26.png`

## Билет 20
- `image15.png`
  - После: 21 Декомпозиция задачи сетевого взаимодействия. Иерархия протоколов. Модель ISO/OSI.

## Билет 21
- `image63.png`
  - До: Интерфейс - набор правил, обеспечивающих взаимодействие модулей соседних уровней.
  - В абзаце: Из презы
  - После: ИЕРАРХИЯ ПРОТОКОЛОВ | Из инета

## Билет 21
- `image137.png`
  - До: Формализованные правила, определяющие последовательность и формат сообщений, которыми обмениваются сетевые компоненты, лежащие на одном уровне, но в разных узлах, называются протоколом. | Интерфейс определяет последовательность и формат сообщений, которыми обмениваются сетевые компоненты, лежащие на соседних уровнях в одном узле, и набор услуг, предоставляемый данным уровнем соседнему уровню.
  - После: Протоколы определяют правила взаимодействия модулей одного уровня в разных узлах, а интерфейсы – модулей соседних уровней в одном узле. | Иерархически организованный набор протоколов, достаточный для организации взаимодействия узлов в сети, называется стеком коммуникационных протоколов.

## Билет 21
- `image119.png`
  - До: Иерархически организованный набор протоколов, достаточный для организации взаимодействия узлов в сети, называется стеком коммуникационных протоколов. | Из презы
  - После: МОДЕЛЬ OSI/ISO

## Билет 21
- `image153.png`
  - До: МОДЕЛЬ OSI/ISO
- `image17.png`
  - До: МОДЕЛЬ OSI/ISO

## Билет 22
- `image94.png`
  - До: 22 Уровни модели ISO/OSI: физический уровень, канальный уровень, сетевой уровень.
  - После: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.
- `image134.png`
  - До: 22 Уровни модели ISO/OSI: физический уровень, канальный уровень, сетевой уровень.
  - После: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.

## Билет 22
- `image155.png`
  - До: 22 Уровни модели ISO/OSI: физический уровень, канальный уровень, сетевой уровень.
  - После: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.

## Билет 23
- `image11.png`
  - До: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.
  - После: 24 Стек TCP/IP. Сравнение с ISO/OSI.
- `image43.png`
  - До: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.
  - После: 24 Стек TCP/IP. Сравнение с ISO/OSI.
- `image141.png`
  - До: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.
  - После: 24 Стек TCP/IP. Сравнение с ISO/OSI.

## Билет 23
- `image131.png`
  - До: 23 Уровни модели ISO/OSI: транспортный уровень, сеансовый уровень, уровень представления, прикладной уровень.
  - После: 24 Стек TCP/IP. Сравнение с ISO/OSI.

## Билет 24
- `image67.png`
  - До: TCP/IP следует принципу "сквозной" связи (end-to-end): сложная логика (надежность, шифрование) реализуется на концах канала (хостах), а сеть (ядро) стремится быть простой и быстрой. | OSI изначально предполагала больше "интеллекта" внутри сети (например, исправление ошибок на канальном уровне).

## Билет 25
- `image25.png`
  - До: используются разные пары проводов; | среда всегда доступна.
  - После: Поле EtherType содержит информацию о протоколе вышестоящего уровня | MAC-адреса - уникальный адрес сетевого устройства, состоящий из 48 бит и принадлажащий непосредственно устройству сетевому (сетевой карте), а не ОС. Состоит из двух частей:

## Билет 26
- `image4.png`
  - До: 26 Повторитель. Мост. Принцип работы моста. Адресная таблица. | HUB
  - После: Hub - устройство L1 модели OSI

## Билет 26
- `image149.png`
  - До: МОСТ | Network bridge – это сетевое устройство, предназначенное для объединения сегментов сети передачи данных в единую сеть. Он работает на канальном (втором) уровне модели OSI (модели взаимодействия открытых систем). В отличие от концентратора, который работает на физическом уровне, сетевой мост не просто транслирует полученные с одного порта устройства на другие, а анализирует заголовок и отправляет на какой-либо один порт, либо не передает ни куда.
  - После: Благодаря тому, что сетевой мост фильтрует пакеты в соответствии с адресом получателя, в сети Ethernet тем самым предотвращается распространение коллизий, т.е. отдельные сети, подключаемые к портам образуют изолированные сегменты. Это позволяет увеличить скорость передачи данных и емкость в каждой отдельной сети.

## Билет 26
- `image124.png`
  - До: Благодаря тому, что сетевой мост фильтрует пакеты в соответствии с адресом получателя, в сети Ethernet тем самым предотвращается распространение коллизий, т.е. отдельные сети, подключаемые к портам образуют изолированные сегменты. Это позволяет увеличить скорость передачи данных и емкость в каждой отдельной сети.
  - После: 27 Коммутатор. Перегрузки и способы борьбы с ними. Архитектуры коммутаторов. Петли.

## Билет 27
- `image113.png`
  - До: 27 Коммутатор. Перегрузки и способы борьбы с ними. Архитектуры коммутаторов. Петли.
  - После: VLAN — это технология логического разделения одной физической Ethernet-сети на несколько независимых широковещательных доменов. Устройства в разных VLAN не обмениваются кадрами напрямую, даже если подключены к одному коммутатору.

## Билет 27
- `image120.png`
  - До: видеоконференций; | потокового мультимедиа.
  - После: 1ый способ борьбы с перегрузками

## Билет 27
- `image69.png`
  - До: 1ый способ борьбы с перегрузками
  - После: 2ой способ борьбы с перегрузками

## Билет 27
- `image31.png`
  - До: 2ой способ борьбы с перегрузками
  - После: Решение проблемы петель - не использовать петли (с) Дима

## Билет 27
- `image47.png`
  - До: 2ой способ борьбы с перегрузками
  - После: Решение проблемы петель - не использовать петли (с) Дима
- `image65.png`
  - До: 2ой способ борьбы с перегрузками
  - После: Решение проблемы петель - не использовать петли (с) Дима

## Билет 28
- `image54.png`
  - До: 28 Типы сетевых адресов. IP-адрес. Маски. Особые IP адреса.
  - После: (информация о длине актуальная только для IPv4. У IPv6 адреса длина 128 бит)

## Билет 28
- `image148.png`
  - До: 28 Типы сетевых адресов. IP-адрес. Маски. Особые IP адреса.
  - После: (информация о длине актуальная только для IPv4. У IPv6 адреса длина 128 бит)

## Билет 28
- `image59.png`
  - До: (информация о длине актуальная только для IPv4. У IPv6 адреса длина 128 бит)
  - После: 29 Отображение IP адресов на аппаратные. ARP протокол.

## Билет 28
- `image50.png`
  - После: 29 Отображение IP адресов на аппаратные. ARP протокол. | Для маршрутизации трафика между разными сетями маршрутизатор использует IP адреса. Но IP адрес - логический адрес сетевого устройства, когда как MAC адрес физический адрес. Для сопоставления IP адреса и MAC адреса (нахождения MAC адреса по IP адресу) используется протокол ARP.

## Билет 29
- `image133.png`
  - До: 29 Отображение IP адресов на аппаратные. ARP протокол. | Для маршрутизации трафика между разными сетями маршрутизатор использует IP адреса. Но IP адрес - логический адрес сетевого устройства, когда как MAC адрес физический адрес. Для сопоставления IP адреса и MAC адреса (нахождения MAC адреса по IP адресу) используется протокол ARP.
  - После: 30 Доменные адреса. DNS. DHCP.

## Билет 30
- `image27.png`
  - До: Доменные имена - символьные идентификаторы сетевых интерфейсов. Строится по иерархическому принципу (от имени хоста до домена самого высокого уровня)
  - После: Суть рекурсивного алгоритма

## Билет 30
- `image160.png`
  - После: Суть рекурсивного алгоритма | Рекурсивное разрешение — это режим, при котором DNS-сервер берёт на себя полную ответственность за получение окончательного ответа.

## Билет 30
- `image55.png`
  - До: Клиент обращается к следующему серверу. | Процесс повторяется до получения авторитетного ответа.
  - После: 1) Обнаружение (Discover). После подключения клиента начинается процесс его инициализации в сети. Он находит подходящий DHCP-сервер путем отправки специального запроса DHCPDISCOVER на адрес 255.255.255.255. Учитывая отсутствие собственного IP, в таком запросе указывается 0.0.0.0 и MAC. Запрос поступает на все ПК в соответствующем сегменте сети. При этом ответ на него автоматически отправляется только DHCP-серверами. | 2) Предложение (Offer). Получив от клиента запрос, DHCP-сервер осуществляет его обработку и выполняет подбор сетевую конфигурацию. Эта конфигурация направляется клиенту в обратном сообщении DHCPOFFER, которое, как правило, передается на указанный MAC. Однако в некоторых случаях применяется широковещание. При нахождении нескольких серверов в пределах сети клиенту приходит соответствующее количество DHCPOFFER, из которых он выбирает один (обычно первый по времени получения).

## Билет 31
- `image95.png`
  - До: 31 Маршрутизация пакетов. Таблицы маршрутизации. Выбор маршрута.

## Билет 31
- `image82.png`
  - До: 31 Маршрутизация пакетов. Таблицы маршрутизации. Выбор маршрута.
  - После: IP-адрес интерфейса маршрутизатора — это адрес, назначенный конкретному сетевому интерфейсу и принадлежащий определённой IP-подсети. Он не является адресом сети, а используется для взаимодействия маршрутизатора с узлами данной сети и как адрес шлюза по умолчанию.

## Билет 31
- `image97.png`
  - После: IP-адрес интерфейса маршрутизатора — это адрес, назначенный конкретному сетевому интерфейсу и принадлежащий определённой IP-подсети. Он не является адресом сети, а используется для взаимодействия маршрутизатора с узлами данной сети и как адрес шлюза по умолчанию.

## Билет 31
- `image42.png`
  - До: IP-адрес интерфейса маршрутизатора — это адрес, назначенный конкретному сетевому интерфейсу и принадлежащий определённой IP-подсети. Он не является адресом сети, а используется для взаимодействия маршрутизатора с узлами данной сети и как адрес шлюза по умолчанию.
  - После: Маршрут до хоста - маршрут к конкретному IP-адресу. | Применение: точечная маршрутизация; политика безопасности; VPN, loopback-интерфейсы.

## Билет 31
- `image56.png`
  - До: Типы маршрутов до сети:
  - После: 32 Протокол ICMP. Утилиты ping и traceroute.

## Билет 31
- `image132.png`
  - После: 32 Протокол ICMP. Утилиты ping и traceroute.

## Билет 32
- `image103.png`
  - До: 32 Протокол ICMP. Утилиты ping и traceroute.
  - После: traceroute утилита используется для составления полного маршрута до узла - списка IP адресов устройств/маршрутизаторов, через которые проходит IP пакет до адреса назначения. | УТИЛИТА PING
- `image85.png`
  - До: 32 Протокол ICMP. Утилиты ping и traceroute.
  - После: traceroute утилита используется для составления полного маршрута до узла - списка IP адресов устройств/маршрутизаторов, через которые проходит IP пакет до адреса назначения. | УТИЛИТА PING

## Билет 33
- `image8.png`
  - До: 33 Транспортные протоколы TCP и UDP. Особенности и сравнение протоколов. | TCP, UDP - протоколы транспортного уровня (в моделях OSI и TCP/IP)
  - После: Применение - онлайн-игры, потоковое видео/аудио (VoIP, IPTV), DNS, DHCP.

## Билет 33
- `image1.png`
  - До: TCP, UDP - протоколы транспортного уровня (в моделях OSI и TCP/IP)
  - После: Применение - онлайн-игры, потоковое видео/аудио (VoIP, IPTV), DNS, DHCP.

## Билет 33
- `image18.png`
  - После: Применение - веб-серфинг (HTTP/HTTPS), электронная почта (SMTP, POP3), файловые передачи (FTP, SFTP).

## Билет 33
- `image57.png`
  - До: Применение - веб-серфинг (HTTP/HTTPS), электронная почта (SMTP, POP3), файловые передачи (FTP, SFTP).
  - После: 34 Принципы работы TCP протокола. Способ организации гарантии передачи данных.

## Билет 34
- `image98.png`
  - До: Начинается с установления TCP соединения.
  - После: TLS HANDSHAKE - установление защищенного (SSL сертификатами) соединениям

## Билет 34
- `image144.png`
  - До: TLS HANDSHAKE - установление защищенного (SSL сертификатами) соединениям
  - После: Алгоритм TLS 1.2 Handshake: | 1) Сообщение "hello" клиента: Клиент инициирует рукопожатие, посылая серверу сообщение "hello". В сообщении будет указано, какую версию TLS поддерживает клиент, поддерживаемые наборы шифров и строка случайных байтов, известная как "random клиента".

## Билет 34
- `image101.png`
  - До: НАЧАЛО ПРО ОРГАНИЗАЦИЮ ПЕРЕДАЧИ ДАННЫХ В TCP
  - После: МЕТОД ПРОСТОЯ ИСТОЧНИКА

## Билет 34
- `image142.png`
  - До: МЕТОД ПРОСТОЯ ИСТОЧНИКА
  - В абзаце: Метод с простоями требует, чтобы источник, пославший кадр, ожи­дал получения квитанции (положительной или отрицательной) от приемника и только после этого посылал следующий кадр (или повторял искаженный). Если же квитан­ция не приходит в течение тайм-аута, то кадр (или квитанция) считается утерянным и его передача повторяется.
  - После: Квитанция (подтверждение) посылается только в случае правильного приема данных, отрицательные квитанции не посылаются. В качестве квитанции получатель сегмента отсылает ответное сообщение (сегмент), в которое помещает число, на единицу превышающее максимальный номер байта в полученном сегменте.

## Билет 34
- `image138.png`
  - До: Квитанция (подтверждение) посылается только в случае правильного приема данных, отрицательные квитанции не посылаются. В качестве квитанции получатель сегмента отсылает ответное сообщение (сегмент), в которое помещает число, на единицу превышающее максимальный номер байта в полученном сегменте.

## Билет 34
- `image102.png`
  - После: 35 VPN. Принципы работы. Области применения.

## Билет 35
- `image121.png`
  - До: 35 VPN. Принципы работы. Области применения.
  - После: ПРИНЦИП РАБОТЫ VPN

## Билет 35
- `image36.png`
  - До: 35 VPN. Принципы работы. Области применения.
  - После: ПРИНЦИП РАБОТЫ VPN | основан на создании зашифрованного туннеля между вашим устройством и удаленным VPN-сервером, который выступает посредником для вашего интернет-трафика, маскируя ваш реальный IP-адрес и защищая данные от перехвата и чтения третьими лицами. Данные шифруются до отправки и расшифровываются только на сервере, а затем запросы к сайтам идут с IP-адресом сервера, обеспечивая анонимность и обход блокировок.

## Билет 35
- `image66.png`
  - До: В ОБЩИХ ЧЕРТАХ КАК РАБОТАТЬ С VPN
  - После: ОБЛАСТИ ПРИМЕНЕНИЯ (в компаниях)

## Билет 35
- `image16.png`
  - До: ОБЛАСТИ ПРИМЕНЕНИЯ (в компаниях)

## Билет 35
- `image126.png`
  - До: ОБЛАСТИ ПРИМЕНЕНИЯ (в компаниях)
  - После: VPN Extranet = VPN Intranet + Клиенты/внешние компании

## Билет 35
- `image99.png`
  - После: VPN Extranet = VPN Intranet + Клиенты/внешние компании

## Билет 36
- `image104.png`
  - До: Обход цензуры: Предоставляет доступ к заблокированным в стране ресурсам и социальным сетям. | 36 Веб-службы. Страницы. Модель клиент сервер. URL.
  - После: Веб-служба, веб-сервис — идентифицируемая уникальным веб-адресом программная система со стандартизированными интерфейсами.

## Билет 36
- `image6.png`
  - До: Веб-служба, веб-сервис — идентифицируемая уникальным веб-адресом программная система со стандартизированными интерфейсами.

## Билет 36
- `image156.png`
  - До: Веб-служба, веб-сервис — идентифицируемая уникальным веб-адресом программная система со стандартизированными интерфейсами.

## Билет 36
- `image130.png`
  - После: 37 HTTP протокол. Формат HTTP сообщения (составные части).

## Билет 40
- `image38.png`
  - До: Обеспечение отказоустойчивости не должно быть дороже, чем вероятный простой. | 40 Отказоустойчивость отдельных компонент: память, устройства хранения, сетевые интерфейсы.

## Билет 40
- `image93.png`
  - До: 40 Отказоустойчивость отдельных компонент: память, устройства хранения, сетевые интерфейсы.

## Билет 40
- `image13.png`

## Билет 40
- `image79.png`

## Билет 40
- `image140.png`

## Билет 40
- `image111.png`

## Билет 40
- `image34.png`
  - После: 41 Отказоустойчивость сетевой инфраструктуры.

## Билет 40
- `image7.png`
  - После: 41 Отказоустойчивость сетевой инфраструктуры.

## Билет 41
- `image77.png`
  - До: 41 Отказоустойчивость сетевой инфраструктуры.
  - После: 42 Обеспечение отказоустойчивости хранения и передачи данных. Кластерные и распределенные файловые системы.

## Билет 41
- `image10.png`
  - До: 41 Отказоустойчивость сетевой инфраструктуры.
  - После: 42 Обеспечение отказоустойчивости хранения и передачи данных. Кластерные и распределенные файловые системы.

## Билет 42
- `image125.png`
  - До: 42 Обеспечение отказоустойчивости хранения и передачи данных. Кластерные и распределенные файловые системы.

## Билет 42
- `image39.png`
  - До: 42 Обеспечение отказоустойчивости хранения и передачи данных. Кластерные и распределенные файловые системы.

## Билет 42
- `image73.png`

## Билет 42
- `image88.png`
  - После: Кластерная система (Cluster) содержит несколько серверов в общей сети.

## Билет 42
- `image100.png`
  - После: Кластерная система (Cluster) содержит несколько серверов в общей сети. | Распределенная система (Distributed) содержит несколько серверов, объединенных по глобальной сети WAN (Wide area Network).

## Билет 43
- `image108.png`
  - До: 43 Отказоустойчивость уровня хоста: кластеры. Active-Backup, N+1, Active-Active.

## Билет 43
- `image157.png`
  - До: 43 Отказоустойчивость уровня хоста: кластеры. Active-Backup, N+1, Active-Active.

## Билет 43
- `image12.png`

## Билет 43
- `image33.png`
  - После: 44 Состояние Split-brain: чем опасно и как бороться.

## Билет 44
- `image61.png`
  - До: 44 Состояние Split-brain: чем опасно и как бороться.
  - После: «Split Brain» — это критическое состояние в распределенных системах (кластерах серверов), когда связь между узлами нарушается, и каждый узел ошибочно полагает, что он единственный активный, что приводит к несогласованности данных и хаосу

## Билет 44
- `image154.png`
  - До: «Split Brain» — это критическое состояние в распределенных системах (кластерах серверов), когда связь между узлами нарушается, и каждый узел ошибочно полагает, что он единственный активный, что приводит к несогласованности данных и хаосу
  - После: Quorum — это правило - кластер считается рабочим только если доступно большинство узлов.

## Билет 44
- `image151.png`
  - До: «Split Brain» — это критическое состояние в распределенных системах (кластерах серверов), когда связь между узлами нарушается, и каждый узел ошибочно полагает, что он единственный активный, что приводит к несогласованности данных и хаосу
  - После: Quorum — это правило - кластер считается рабочим только если доступно большинство узлов.

## Билет 44
- `image118.png`
  - До: Quorum — это правило - кластер считается рабочим только если доступно большинство узлов.
  - После: Fencing — это принудительное отключение или изоляция узла, который: | выпал из синхронизации,

## Билет 44
- `image86.png`
  - До: либо изолирован от shared-ресурсов. | Поэтому: No fencing — no HA

## Билет 45
- `image2.png`
  - До: 45 Виртуализация. Преимущества и недостатки. Требования к приложению.
  - В абзаце: Недостатки виртуализации
  - После: Накладные расходы | Производительность ниже, чем у “голой” ОС

## Билет 45
- `image112.png`
  - До: Возможны уязвимости гипервизора | Нужны дополнительные средства защиты
  - После: ТРЕБОВАНИЙ К ПРИЛОЖЕНИЮ В СЕКЦИИ С ВИРТУАЛИЗАЦИЕЙ НЕТ В ПРЕЗЕ (гпт хз наскок норм выдает, в инете тоже не нашел)

## Билет 45
- `image147.png`
  - До: ТРЕБОВАНИЙ К ПРИЛОЖЕНИЮ В СЕКЦИИ С ВИРТУАЛИЗАЦИЕЙ НЕТ В ПРЕЗЕ (гпт хз наскок норм выдает, в инете тоже не нашел)

## Билет 45
- `image44.png`
  - До: ТРЕБОВАНИЙ К ПРИЛОЖЕНИЮ В СЕКЦИИ С ВИРТУАЛИЗАЦИЕЙ НЕТ В ПРЕЗЕ (гпт хз наскок норм выдает, в инете тоже не нашел)

## Билет 45
- `image110.png`

## Билет 45
- `image76.png`

## Билет 45
- `image48.png`
  - После: 46 Отказоустойчивость БД. Репликация. Типы репликации. Способы организации репликации.

## Билет 46
- `image159.png`
  - До: 46 Отказоустойчивость БД. Репликация. Типы репликации. Способы организации репликации.

## Билет 46
- `image115.png`
  - До: 46 Отказоустойчивость БД. Репликация. Типы репликации. Способы организации репликации.

## Билет 46
- `image37.png`
  - До: ТИПЫ РЕПЛИКАЦИИ ПО СИНХРОНИЗАЦИИ
  - После: ТИПЫ РЕПЛИКАЦИИ ПО СПОСОБУ ПЕРЕДАЧИ | РЕПЛИКАЦИЯ НА ОСНОВЕ WAL

## Билет 46
- `image84.png`
  - До: ТИПЫ РЕПЛИКАЦИИ ПО СПОСОБУ ПЕРЕДАЧИ | РЕПЛИКАЦИЯ НА ОСНОВЕ WAL
  - После: Операторная репликация (пояснение к первому пункту на скрине) | Репликация за счет передачи SQL запросов на реплику.

## Билет 46
- `image29.png`
  - До: БЛОЧНАЯ РЕПЛИКАЦИЯ | При блочной репликации каждая операция записи выполняется не только на основном диске, но и на резервном. Таким образом тому на одном массиве соответствует зеркальный том на другом массиве, с точностью до байта повторяющий основной том
  - После: К достоинствам такой репликации можно отнести простоту настройки и надёжность. Записывать данные на удалённый диск может либо дисковый массив, либо нечто (устройство или программное обеспечение), стоящее между хостом и диском.

## Билет 47
- `image90.png`
  - До: 47 Репликация ведущий - ведомый. Проблемы репликации. Два ведущих узла.
  - После: Запись выполняется в одном узле (master), а чтение можно распределить между многими репликами (slaves). | Плюсы

## Билет 47
- `image23.png`
  - До: Single Point of Failure (без failover) | Отставание реплик (replication lag)
  - После: Проблем у репликаций МНОГО - смотря по какому признаку мы классифицируем репликации (в прошлом пункте прописаны классификации репликаций по трем признакам и их минусы)

## Билет 47
- `image150.png`
  - До: Проблем у репликаций МНОГО - смотря по какому признаку мы классифицируем репликации (в прошлом пункте прописаны классификации репликаций по трем признакам и их минусы)
  - После: 1) GUID или префикс | Суть

## Билет 48
- `image35.png`
  - До: 48 CAP теорема.
- `image87.png`
  - До: 48 CAP теорема.

## Билет 49
- `image139.png`
  - До: 49 Архитектура веб-сервисов. Монолитные приложения. Сервисная и микросервисная архитектуры.

## Билет 49
- `image28.png`
  - До: 49 Архитектура веб-сервисов. Монолитные приложения. Сервисная и микросервисная архитектуры.
  - После: ИЗ ИНЕТА
- `image80.png`
  - До: 49 Архитектура веб-сервисов. Монолитные приложения. Сервисная и микросервисная архитектуры.
  - После: ИЗ ИНЕТА

## Билет 49
- `image70.png`
  - До: SOA:В его основе лежат несколько основных идей – переиспользование сервисов и корпоративная шина. Разработчики стремятся разбить систему на сервисы таким образом, чтобы их можно было использовать повторно. Взаимодействие и маршрутизация осуществляется через корпоративную шину ESB. Типичная SOA архитектура показана на рисунке ниже
  - После: Шина(ESB): в случае взаимодействия сложных событий действует как посредник и управляет различными рутинными операциями, такими как передача сообщений и координация вызовов.

## Билет 49
- `image62.png`
  - До: МИКРОСЕРВИСЫ: | Микросервисы в отличие от SOA, наоборот, избегают повторного использования, применяя философию - предпочтительнее дублирование, а не зависимость от других сервисов. Повторное использование предполагает связанность, а архитектура микросервисов в значительной степени старается ее избегать. Это достигается за счет разбиения системы на сервисы по ограниченным контекстам (бизнес областям). Типичная MSA архитектура показана на рисунке ниже.
  - После: В отличие от SOA каждый сервис обладает всеми необходимыми для функционирования частями – имеет свою собственную базу данных и существует как независимый процесс. Такая архитектура делает каждый сервис физически разделенным, самодостаточным, что ведет с технической точки зрения к архитектуре без разделения ресурсов.

## Билет 50
- `image74.png`
  - До: 50 Межпроцессное взаимодействие. Традиционные подходы, SOAP, REST, gRPC.

## Билет 50
- `image75.png`
  - До: 50 Межпроцессное взаимодействие. Традиционные подходы, SOAP, REST, gRPC.
  - После: Некоторые преимущества SOAP, которые делают его подходящим для определённых задач:

## Билет 50
- `image81.png`
  - После: Некоторые преимущества SOAP, которые делают его подходящим для определённых задач: | Многообразие транспортных протоколов. SOAP работает не только через HTTP, но и через другие протоколы, такие как SMTP, что даёт большую гибкость в использовании.

## Билет 50
- `image58.png`
  - До: Большие объёмы сообщений. Использование XML может приводить к избыточным данным в сообщениях, что влияет на производительность. | Зависимость от XML. Необходимость работы с XML-форматом требует дополнительных вычислительных ресурсов.

## Билет 50
- `image41.png`
  - До: Зависимость от XML. Необходимость работы с XML-форматом требует дополнительных вычислительных ресурсов.
  - После: Плюсы:

## Билет 50
- `image20.png`
  - После: Плюсы: | REST API применяет классические HTTP-запросы, поэтому он прост в использовании;

## Билет 50
- `image60.png`
  - До: гибкость: не зависит от языка программирования, платформы и формата данных (часто JSON, XML);

## Билет 50
- `image116.png`
  - После: Плюсы gRPC:1) Использование HTTP/2. Это бинарный протокол - вместо текстового формата HTTP/1.1, HTTP/2 использует бинарное кадрирование. Это компактнее, быстрее для парсинга и передачи, особенно для маленьких запросов. Мультиплексирование: Позволяет отправлять и получать множество запросов и ответов параллельно через одно TCP-соединение. Это устраняет блокировку «голова-в-очереди» (head-of-line blocking), характерную для HTTP/1.1, и значительно снижает задержки.

## Билет 50
- `image106.png`
  - После: Плюсы gRPC:1) Использование HTTP/2. Это бинарный протокол - вместо текстового формата HTTP/1.1, HTTP/2 использует бинарное кадрирование. Это компактнее, быстрее для парсинга и передачи, особенно для маленьких запросов. Мультиплексирование: Позволяет отправлять и получать множество запросов и ответов параллельно через одно TCP-соединение. Это устраняет блокировку «голова-в-очереди» (head-of-line blocking), характерную для HTTP/1.1, и значительно снижает задержки. | 2) Автоматическая генерация кода: Компилятор protoc создает клиенты и серверы на разных языках, сокращая boilerplate-код.

## Билет 51
- `image109.png`
  - До: 51 Масштабирование приложений: горизонтальное, вертикальное. Многоуровневая архитектура.
  - После: 52 Что такое frontend, reverse proxy, load balancer, CDN? Их назначения и принципы работы.

## Билет 51
- `image145.png`
  - До: 51 Масштабирование приложений: горизонтальное, вертикальное. Многоуровневая архитектура.
  - После: 52 Что такое frontend, reverse proxy, load balancer, CDN? Их назначения и принципы работы. | Frontend - это область разработки ПО, которая фокусируется на создании пользовательского интерфейса и взаимодействии пользователя с веб-приложением. К фронтенду относится всё, что пользователи видят на сайте или в приложении, и с чем можно взаимодействовать: текст, изображения, анимации, кнопки, формы, слайдеры, галереи. Обычно frontend проектируется как быстрый и легкий веб-сервер (nginx / haproxy). Web-сервер раздает статику (html + css) - то, что можно отдать клиенту без вычислений на стороне бизнес-логики.Reverse proxy - это сервер, который принимает запросы от пользователей и перенаправляет их на внутренние серверы, скрывая их реальные адреса. Позволяет повысить безопасность путем ограничения списка входящих адресов, настроить HTTPS с помощью SSL сертификатов, добавлять заголовки запросов, сжатие ответов алгоритмами gzip, кеширование и балансировку нагрузки.Load balancer - это инструмент reverse proxy, позволяющий распределять запросы от пользователей на один из списка внутренних серверов в соответствии с алгоритмом для снижения нагрузки. Примеры алгоритмов балансировщика:1. Round-robin - отправляет запросы на сервера по-очереди. После окончания списка начинает сначала;2. Weighted round-robin - то же самое, только каждому серверу из списка присваивается вес. Чем больше вес, тем больше запросов за один “круг” получит сервер;

## Билет 53
- `image22.png`
  - До: 53 Что такое backend. Способы обеспечения производительности и увеличения пропускной способности.
  - После: Оптимизация на уровне кода (Application level) | Алгоритмическая сложность

## Билет 54
- `image122.png`
  - До: 54 Масштабирование хранилища. Секционирование, маршрутизация, ребалансировка.
  - После: Партицонирование (partitioning) ­­или секционирование – это разделение большой таблицы на разделы (партиции, partition), обычно в рамках одной физической БД и на одном сервере. Размещение данных по разделам производится на основании выбранного ключа партиционирования. Например, PostgreSQL из коробки поддерживает разделение по диапазонам, по списку и по хэшу.

## Билет 54
- `image91.png`
  - До: 54 Масштабирование хранилища. Секционирование, маршрутизация, ребалансировка.
  - После: Партицонирование (partitioning) ­­или секционирование – это разделение большой таблицы на разделы (партиции, partition), обычно в рамках одной физической БД и на одном сервере. Размещение данных по разделам производится на основании выбранного ключа партиционирования. Например, PostgreSQL из коробки поддерживает разделение по диапазонам, по списку и по хэшу.

## Билет 54
- `image146.png`
  - До: Однако, этот метод по-прежнему ограничивается ресурсами одного сервера и не повышает отказоустойчивость системы в целом, т.к. в случае сбоя сервера, все разделы становятся недоступны.
  - После: (маршрутизация про шардирование как раз) | Шардирование (sharding) – это разделение данных по нескольким серверам (шардам), каждый из которых хранит часть данных. В отличии от партиционирования, обычно управляется на уровне приложения или специализированного промежуточного программного обеспечения (middleware), требует дополнительной логики маршрутизации запросов и управления узлами. Каждый шард представляет собой независимую базу данных, хранящую часть данных. Поэтому при шардировании данные распределены по нескольким физическим серверам согласно заданному ключу. Решение, на какой шард записать данные или откуда их считать, принимает клиентское приложение или специальный маршрутизатор.

## Билет 54
- `image52.png`
  - До: гибкие возможности эффективной утилизации ресурсов и затрат на инфраструктуру. | Однако, для этого способа повышения производительности БД характерна высокая сложность реализации и обслуживания.
  - После: 55 Масштабирование во времени. AMQP и Kafka.

## Билет 56
- `image19.png`
  - До: Типичное применение: event streaming, аналитика, CDC, логирование. | 56 Понятие observability.
  - После: Observability - это способность системы предоставлять достаточную информацию, чтобы можно было понять, что происходит внутри неё, без необходимости модифицировать исходный код. Состоит из мониторинга, логирования, трейсинга и визуализации. | Мониторинг - Систематические сбор, обработка, агрегирование и отображение в реальном времени количественных показателей системы. Нужен для:

